import { useEffect, useState } from 'react'
import { motion, AnimatePresence } from 'framer-motion'
import { Menu, X } from 'lucide-react'
import BrandLogo from './BrandLogo'

const NAV=[['Concepto','/#about'],['Colección','/#models'],['Todo incluido','/#included'],['Proceso','/#process'],['Contacto','/#contact']]
export default function Navbar({forceLight=false}){const [scrolled,setScrolled]=useState(forceLight),[open,setOpen]=useState(false);useEffect(()=>{if(forceLight){setScrolled(true);return}const fn=()=>setScrolled(window.scrollY>40);fn();window.addEventListener('scroll',fn);return()=>window.removeEventListener('scroll',fn)},[forceLight]);return <><motion.header initial={{y:-70,opacity:0}} animate={{y:0,opacity:1}} transition={{duration:.8,ease:[.16,1,.3,1]}} className={`fixed top-0 inset-x-0 z-50 transition-all duration-500 ${scrolled?'py-3 bg-cream-200/90 backdrop-blur-xl border-b border-navy-700/10':'py-5 bg-transparent'}`}><div className="max-w-[1600px] mx-auto px-6 md:px-12 flex items-center justify-between"><a href="/" aria-label="VORA inicio"><BrandLogo light={!scrolled}/></a><nav className="hidden lg:flex items-center gap-9">{NAV.map(([label,href])=><a key={href} href={href} className={`text-sm link-underline ${scrolled?'text-navy-700/80':'text-cream-200/85'}`}>{label}</a>)}</nav><div className="flex items-center gap-4"><a href="/#contact" className={`hidden md:inline-flex items-center gap-2 text-sm font-medium ${scrolled?'text-navy-700':'text-cream-200'}`}><span className="w-1.5 h-1.5 rounded-full bg-gold-400"/> Estudiar mi parcela</a><button onClick={()=>setOpen(true)} className={`lg:hidden w-10 h-10 flex items-center justify-center rounded-full border ${scrolled?'border-navy-700/20 text-navy-700':'border-cream-200/30 text-cream-200'}`} aria-label="Abrir menú"><Menu size={18}/></button></div></div></motion.header><AnimatePresence>{open&&<motion.div initial={{clipPath:'inset(0 0 100% 0)'}} animate={{clipPath:'inset(0)'}} exit={{clipPath:'inset(0 0 100% 0)'}} transition={{duration:.65,ease:[.87,0,.13,1]}} className="fixed inset-0 z-[60] bg-navy-800 text-cream-200 flex flex-col"><div className="flex items-center justify-between p-6"><BrandLogo light/><button onClick={()=>setOpen(false)} className="w-10 h-10 rounded-full border border-cream-200/20 flex items-center justify-center"><X size={18}/></button></div><div className="flex-1 flex flex-col justify-center px-6 gap-4">{NAV.map(([label,href],i)=><motion.a key={href} href={href} initial={{y:35,opacity:0}} animate={{y:0,opacity:1}} transition={{delay:.15+i*.06}} className="font-display text-5xl tracking-tightest">{label}</motion.a>)}</div><div className="p-6 border-t border-cream-200/10 section-label text-cream-200/50">VORA · CONCRETE LIVING</div></motion.div>}</AnimatePresence></>}
